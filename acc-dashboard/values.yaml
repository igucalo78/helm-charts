druid:
  kafkaUrl: "{{ .Release.Name }}-kafka:9092"
nkafka-helm:
  # version: latest
  kafkaUrl: "{{ .Release.Name }}-kafka:9092"
  # natsUrl: "nats://nats-service:4222"
# acc-sys-dashboard:
#   metrics-server:
#     rbac:
#       create: false
