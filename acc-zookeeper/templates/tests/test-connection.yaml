apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acc-zookeeper.fullname" . }}-test-connection"
  labels:
    {{- include "acc-zookeeper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "acc-zookeeper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
