apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cw-phy-cell.fullname" . }}-bootstrap
  labels:
    {{- include "cw-phy-cell.labels" . | nindent 4 }}
data:
  bootstrap.txt: |
    redis.hostname:{{ tpl .Values.redisConfig.hostname . }}
    redis.port:{{ tpl .Values.redisConfig.port . }}
    instance.filter:{{ tpl .Values.bootstrapId . }}
