apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-confgimaps.fullname" . }}
  labels:
    {{- include "test-confgimaps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-confgimaps.selectorLabels" . | nindent 4 }}
