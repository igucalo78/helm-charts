apiVersion: v1
kind: ConfigMap
metadata:
 name: conf-settings
 labels:
   {{- include "xapp-api.labels" . | nindent 4 }}
data:
  settings.py: |
    REDIS_URL = "{{ tpl .Values.redis.url . }}"
    REDIS_PORT = 6379
