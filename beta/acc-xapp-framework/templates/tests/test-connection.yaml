apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acc-xapp-framework.fullname" . }}-test-connection"
  labels:
    {{- include "acc-xapp-framework.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "acc-xapp-framework.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
