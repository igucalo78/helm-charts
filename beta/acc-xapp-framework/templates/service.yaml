apiVersion: v1
kind: Service
metadata:
  name: {{ include "acc-xapp-framework.fullname" . }}
  labels:
    {{- include "acc-xapp-framework.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acc-xapp-framework.selectorLabels" . | nindent 4 }}
